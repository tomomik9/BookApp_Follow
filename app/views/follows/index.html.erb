<h1 class="users"><%= @user.username %></h1>
<h2 class="users"><%= t('.Follow') %></h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to "#{user.username}", user_path(user) %>
     </li>
  <% end %>
</ul>
</br>
<%= link_to t('books.index.Users'), users_path %>
